# Disable optional dependencies that cause platform issues
optional=false

# Force use of esbuild instead of platform-specific Rollup
rollup-platform-override=true

# Ensure clean installs
package-lock=false 